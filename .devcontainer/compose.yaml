services:
  py311:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.py311
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    # Optional: Create .env from .env.example to configure TRIAGE_PROVIDER, tokens, etc.
    # If .env exists, environment variables will be loaded automatically

  py312:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.py312
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    # Optional: Create .env from .env.example to configure TRIAGE_PROVIDER, tokens, etc.
    # If .env exists, environment variables will be loaded automatically
