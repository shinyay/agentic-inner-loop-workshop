services:
  py311:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.py311
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    # Optional: Create .env from .env.example in the repository root to configure
    # TRIAGE_PROVIDER, tokens, etc. Docker Compose will load it if it exists.

  py312:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.py312
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    # Optional: Create .env from .env.example in the repository root to configure
    # TRIAGE_PROVIDER, tokens, etc. Docker Compose will load it if it exists.
